{
  "name": "@glyph/adapters",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "bench": "node ./scripts/bench.mjs"
  },
  "dependencies": {
    "eventemitter3": "^5.0.1"
  },
  "peerDependencies": {
    "@glyph/core": "*",
    "@glyph/address-mapper": "*",
    "@glyph/glyph-drive": "*"
  }
}
